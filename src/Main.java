public class Main {

    public static void main(String[] args) {
        // We are creating an instance of MyProducts class as "as"
        MyProducts as = new MyProducts();

        // We are calling the start() method from MyProducts class using "as" object
        // to access the start() method within MyProducts class
        as.start();
    }
}